# SVRN Publishing Node

The current Express server is the publishing-node implementation during migration. SVRN v1 discovery and issue endpoints live alongside legacy APIs under `/.well-known/svrn-node.json` and `/svrn/v1`.
