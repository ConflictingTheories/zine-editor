# SVRN Editor

The existing root Vite application is the Editor app during the migration. It now creates and verifies portable `.svrn` archives through `@svrn/format`; this package provides the independent release boundary while shared editor components are moved incrementally.
