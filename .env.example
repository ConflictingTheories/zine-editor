# Backend Configuration
NODE_ENV=development
PORT=3000
JWT_SECRET=change_this_to_a_secure_random_string

# Frontend Configuration
# Use /api if relying on Nginx proxy (recommended for Docker)
# Use http://localhost:3000 if running locally without Nginx proxy
VITE_API_BASE_URL=/api

# Payment Integration (Stripe)
STRIPE_SECRET_KEY=sk_test_...
VITE_STRIPE_PUBLIC_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Crypto Integration (XRPL)
# Use wss://s.altnet.rippletest.net:51233 for Testnet
# Use wss://xrplcluster.com for Mainnet
VITE_XRPL_SERVER=wss://s.altnet.rippletest.net:51233
XRPL_NODE_URL=wss://s.altnet.rippletest.net:51233

# Platform Wallet (Must be funded on the network)
PLATFORM_WALLET_SEED=sEd...

# Database (Supabase / Postgres)
# VITE_SUPABASE_URL=https://...
# VITE_SUPABASE_ANON_KEY=...