# Backend Configuration
NODE_ENV=production
PORT=3000
JWT_SECRET=change_this_to_a_secure_random_string

# Frontend Configuration
# Use /api if relying on Nginx proxy (recommended for Docker)
# Use http://localhost:3000 if running locally without Nginx proxy
VITE_API_BASE_URL=/api

# Optional: Payment Integration
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=